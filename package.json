{
  "name": "threads-orchestrator",
  "version": "0.2.0",
  "description": "Voice-first orchestration for Claude agents and documents in Obsidian or Geode",
  "main": "dist/main.js",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "onnxruntime-web": "^1.26.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.28.2",
    "obsidian": "^1.7.2",
    "typescript": "^5.0.0",
    "vitest": "^4.1.7",
    "ws": "^8.21.0"
  }
}
